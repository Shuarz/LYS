#include <stdio.h>
#include <stdlib.h>

void iniciarArray(/*esto un array*/int parametroArray[], /*esto es un vector*/ int cantidadVector/*esto es un paramatro*/, int valorDeInicio);
void mostarArray(int parametroArray[], int cantidadVector);

int main()
{

    return 0;
}

void mostarArray(int parametroArray[], int cantidadVector)

{
    for( ;cantidadVector>0;cantidadVector--)
    {
        valorDeInicio = 0
        int inicio;
        parametroArray[inicio]==valorDeInicio;
    }
}


